# =========================
# Stage 1 — Builder
# =========================
FROM python:3.12-alpine AS builder

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

# Install build dependencies (temporary)
RUN apk add --no-cache --virtual .build-deps \
    build-base linux-headers libffi-dev

# Copy requirements and install into a temporary directory
COPY requirements.txt .
RUN pip install --no-cache-dir --prefix=/install -r requirements.txt && \
    pip install --no-cache-dir --prefix=/install gunicorn

# =========================
# Stage 2 — Runtime
# =========================
FROM python:3.12-alpine

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PORT=5000

WORKDIR /app

# Only install runtime system dependencies
RUN apk add --no-cache tini

# Copy dependencies from builder
COPY --from=builder /install /usr/local

# Copy Flask application files
COPY . .

# Copy entrypoint script
COPY entrypoint.sh .
RUN chmod +x entrypoint.sh

# Expose port
EXPOSE ${PORT}

# Use Tini and entrypoint script
ENTRYPOINT ["/sbin/tini", "--", "./entrypoint.sh"]

# Run gunicorn server for Flask
CMD ["sh", "-c", "gunicorn --workers 2 --bind 0.0.0.0:${PORT} flaskblog:app"]
